sudo: required
dist: trusty
language: node_js
node_js:
  - "6.9.4"

before_install:
 - export CHROME_BIN=/usr/bin/google-chrome
 - export DISPLAY=:99.0
 - sh -e /etc/init.d/xvfb start
 - sudo apt-get update
 - sudo apt-get install -y libappindicator1 fonts-liberation
 - wget https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb
 - sudo dpkg -i google-chrome*.deb


cache:
  directories:
    - node_modules

install:
  - npm install

script:
  - npm run deploy:prod

after_success:
  - npm run codecov

deploy:
  provider: heroku
  api_key: ab96e7e4-7e36-4133-8837-3714be7a0d48
  app: openclub-web

notifications:
  slack: openclub:1W4Wix44Cr3AHWfkdx0ALrHs
