import { connect } from 'react-redux';
import Ele from '../components/Create';

const mapDispatchToProps = {};

const mapStateToProps = () => {
  return {
  }
}

export default connect(mapStateToProps, mapDispatchToProps)(Ele);
