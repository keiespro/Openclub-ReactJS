import { connect } from 'react-redux'
import FinancesView from '../components/FinancesView'

const mapDispatchToProps = {}

const mapStateToProps = () => {}

export default connect(mapStateToProps, mapDispatchToProps)(FinancesView)
